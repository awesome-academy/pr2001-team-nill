<% provide(:title, 'All Users') %>
<h1>All Users</h1>
<%= will_paginate %>
<table class="table">
  <tr>
    <th>Name</th>
	</tr>
	<% @users.each do |user| %>
	`	<tr>
			<%= link_to user.name, admin_user_path(user) %>
		</tr>
	<% end %>`
</table>
<%= will_paginate %>