<% provide(:title, 'All books') %>
<h1>All Books</h1>
<%= will_paginate %>
<table class="table">
	<tr>
		<th>Name</th>
	</tr>
	<% @books.each do |book| %>
		<tr>
			<td>
				<%= link_to book.name, admin_user_book_path(@user ,   book) %>
			</td>
		</tr>
	<% end %>
</table>
<%= will_paginate %>