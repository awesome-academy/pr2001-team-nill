<% provide(:title, @category.name) %>
<div class="row">
	<aside class="col-md-4">
		<section>
			<h1>
				<%= @category.name %>
				<% @category.dishes.each do |dish| %>
					<%= dish.name %>
				<% end %>
			</h1>
		</section>
	</aside>
</div>